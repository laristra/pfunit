
! Maybe here we can have compound predicates.

module Verifier_mod

  use Predicate_mod
  implicit none
  private

!  type :: Verifier
!   contains
!     procedure :: verify
!  end type Verifier

contains

!  logical function verify(this) result(ok)
!    !  Events?  Subject type?
!    class(Verifier), intent(inout) :: this
!    ok = .false.
!  end function verify

end module Verifier_mod
